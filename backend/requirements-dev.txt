# Development / test dependencies (not needed at runtime).
# Install with: pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0.0
pytest-cov>=5.0.0
# Embedded PostgreSQL for the integration/RLS test suite when TEST_DATABASE_URL
# is not provided (CI provides a service container instead).
pgserver>=0.1.4
# Lint + security scanning (mirrors CI).
ruff>=0.6.0
pip-audit>=2.7.0
